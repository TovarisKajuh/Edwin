{
  "name": "edwin",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel -r run dev",
    "dev:server": "pnpm --filter @edwin/server run dev",
    "dev:web": "pnpm --filter @edwin/web run dev",
    "build": "pnpm -r run build",
    "test": "pnpm -r run test"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild",
      "sharp",
      "unrs-resolver"
    ]
  }
}
